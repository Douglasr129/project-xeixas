{
  "collectionName": "components_menu_links",
  "info": {
    "displayName": "link",
    "icon": "bulletList"
  },
  "options": {},
  "attributes": {
    "link_text": {
      "type": "string",
      "required": true,
      "regex": "^[a-zA-Z]+([0-9-]*[a-zA-Z]+)*$",
      "maxLength": 50
    },
    "url": {
      "type": "string",
      "required": true,
      "maxLength": 255,
      "minLength": 3,
      "regex": "^[a-zA-Z]+([0-9-]*[a-zA-Z]+)*$"
    },
    "open_in_new_tab": {
      "type": "boolean",
      "default": false,
      "required": true
    }
  }
}
